# Live — allow all search engines, crawlers and AI agents full access.
# A blanket allow covers Googlebot, Bingbot, the social preview bots and
# AI crawlers (GPTBot, ClaudeBot, Google-Extended, PerplexityBot, etc.).
User-agent: *
Allow: /
